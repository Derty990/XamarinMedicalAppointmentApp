<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MedicalAppointmentApp.XamarinApp.ViewModels"
             xmlns:model="clr-namespace:MedicalAppointmentApp.XamarinApp.ApiClient" x:Class="MedicalAppointmentApp.Views.AppointmentStatusesPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Dodaj" Command="{Binding AddStatusCommand}" />
    </ContentPage.ToolbarItems>

    <StackLayout>
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#3498db" HorizontalOptions="Center" Margin="10"/>

        <CollectionView ItemsSource="{Binding Statuses}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedStatus, Mode=TwoWay}"
                        SelectionChangedCommand="{Binding EditStatusCommand}"
                        SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:AppointmentStatusForView">
                    <StackLayout Padding="15,10">
                        <Label Text="{Binding StatusName}" FontSize="Medium"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <StackLayout Padding="20" HorizontalOptions="Center" VerticalOptions="Center">
                    <Label Text="Brak zdefiniowanych statusów wizyt." HorizontalTextAlignment="Center" TextColor="Gray"/>
                    <Button Text="Odśwież" Command="{Binding LoadStatusesCommand}" Margin="0,10,0,0"/>
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <Button Text="Odśwież Listę" Command="{Binding LoadStatusesCommand}" Margin="10"/>
    </StackLayout>
</ContentPage>